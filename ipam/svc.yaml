apiVersion: v1
kind: Service
metadata:
  annotations:
    cis.f5.com/ipamLabel: Dev
  labels:
    app: app
  name: app-svc
  namespace: test102
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: app
  sessionAffinity: None
  type: LoadBalancer
